function tenTime(number) {
    const result= number*10;
    return result;
}
function catHalf(number) {
    const half = number/2;
    return half;
}

tenTime(20);

const output= tenTime(20)
console.log(output);
